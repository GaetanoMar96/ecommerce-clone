export interface PriceRange { 
    min?: number;
    max?: number;
}

export type CheckBoxColor = {
    [key: string]: boolean;
};

export interface CheckBoxBrand { 
    nike?: boolean;
    converse?: boolean;
    adidas?: boolean;
}